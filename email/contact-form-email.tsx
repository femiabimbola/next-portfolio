interface ContactFormEmailProps {
  message: string[];
}

const ContactFormEmail = ({message}: ContactFormEmailProps) => {
  return <div> Contact Form Email</div>;
};

export default ContactFormEmail;
